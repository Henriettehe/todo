export default function Navbar({username}) {
    return (
        <header>
            <nav>
                <h1>
                    Hiø
                </h1>
                <span>{username}</span>
            </nav>
       </header>
        
    )
}